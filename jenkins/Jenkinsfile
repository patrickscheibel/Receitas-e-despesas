#!/usr/bin/env groovy
pipeline {
    agent none
    stages {
        stage('Build') {
            agent {
                docker { image 'php:8.1.0-alpine' }
            }
            steps {
                sh "composer install && docker-compose up -d && vendor/bin/phinx migrate -e development && php --version"
            }
        }
    }
}
