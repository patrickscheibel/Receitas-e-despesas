#!/usr/bin/env groovy
pipeline {
    stages {
        stage('build') {
            agent {
                dockerfile true
            }
            steps {
                sh 'composer install'
            }
        }
        stage('database') {
            steps {
                sh 'docker-compose up -d'
            }
        }
        stage('migrations') {
            steps {
                sh 'vendor/bin/phpunit src/tests'
            }
        }
        stage('tests') {
            steps {
                sh 'vendor/bin/phpunit src/tests'
            }
        }
        stage('start') {
            steps {
                sh 'php -S localhost:8080'
            }
        }
    }
}
